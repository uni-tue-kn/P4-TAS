# P4-TAS Measurement

This program contains the source code for the third measurement tofino switch.
Compile it with `make compile`, start it with `make start` and run the control plane with `cargo run`.